version: 0.2
phases:
  pre_build:
    commands:
      - echo Installing dependencies...
      - npm ci
  build:
    commands:
      - echo Building...
      - npm run build
  post_build:
    commands:
      - echo Build completed!
artifacts:
  files:
    - "**/*"
  discard-paths: no
